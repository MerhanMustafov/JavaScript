
const form = document.querySelector('.new-topic-border')

// console.log(comments)
async function postRequest(){
    
}

export function showCommentView(target){
    console.log(form)
    // const comments = document.querySelectorAll('.topic-name-wrapper')
    // console.log(comments)
    // const cId = target.id;
    // Array.from(comments).forEach(c => {
    //     if (c.id != cId){
    //         c.remove()
    //     }
    // })
    // console.log(target)
    // form.remove()
}