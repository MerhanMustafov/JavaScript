function calc(first, sec, third){
    let sum = first.length + sec.length + third.length
    let average = Math.floor(sum/3)
    console.log(sum);
    console.log(average);
}

// calc('chocolate', 'ice cream', 'cake')
// calc('pasta', '5', '22.3')